import { Project.User.Permissions } from './project.user.permissions';

describe('Project.User.Permissions', () => {
  it('should create an instance', () => {
    expect(new Project.User.Permissions()).toBeTruthy();
  });
});
